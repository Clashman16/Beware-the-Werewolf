using UnityEngine;

namespace BWW.Behaviours.Characters
{
   public class PrisonnerCollisionBehaviour : CharacterCollisionBehaviour
   {
      public override void OnTriggerEnter(Collider p_collider)
      {
         // We need to check the collision with villgers only
      }
   }
}
